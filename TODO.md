[x] in audit history, make the area "GPT Result:" rendering markdown, like done for notes in single view
[x] in audit history, make the the popping up window draggable to the left
[] in audit history, all tasks are named "Audit Task". probably it would be good to have the user decide theis name when pasted as gpt result
[] in dual view, extend the height of the alignments area and sort the alignments by row number