[x] in audit history, make the area "GPT Result:" rendering markdown, like done for notes in single view
[x] in audit history, make the the popping up window draggable to the left
[x] in audit history, all tasks are named "Audit Task". probably it would be good to have the user decide their name in "AI Alignment Audit" / "Paste GPT Result"
[x] in dual view, extend the height of the alignments area and sort the alignments by row number
[] in "AI Alignment Audit" / "Paste GPT Result": how to keep updated the gpt models? those offered by claude to be included too
[] modify gpt prompt. make return a json grounded to its anchors
[] window "localhost:3030 says..." requires the user to push ok to close the window. make it automatic
[] adapt buildTripleAlignmentQualityAuditPrompt to cases of only 1 translation  against original
[] in AI Alignment Audit / Include surrounding rows: also the original text must adapt to the specified surroundingf rows accordingly